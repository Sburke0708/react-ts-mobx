import styled from 'styled-components';

export default styled.li`
  max-width: 300px;
  display: inline-flex;
  flex-direction: column;
  padding: 30px;
  margin: auto;
  text-align: center;
  > img {
    max-width: 100px;
    margin: 0 auto;
  }
`;